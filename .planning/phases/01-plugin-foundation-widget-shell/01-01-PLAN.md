---
phase: 01-plugin-foundation-widget-shell
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - devsroom-drilldown-mobile-menu.php
  - src/Plugin.php
  - src/Admin/ElementorNotice.php
  - src/Assets/Registrar.php
  - src/Elementor/Widget/DrillDownMenu.php
  - assets/js/ddmm-frontend.js
  - assets/css/ddmm-frontend.css
autonomous: true
requirements:
  - PLUG-01
  - PLUG-02
  - PLUG-03
  - PLUG-04
  - PLUG-05
  - PLUG-06
  - WIDG-01
  - WIDG-02
  - WIDG-03
  - WIDG-04
  - COMP-01
  - COMP-02
  - COMP-05

must_haves:
  truths:
    - "Plugin activates and shows admin notice with link when Elementor is not active"
    - "Widget appears in Elementor editor under the 'Devsroom' category"
    - "Widget icon displays as a custom SVG hamburger in the Elementor panel"
    - "Widget registers on elementor/widgets/register hook (not deprecated hook)"
    - "Plugin JS and CSS files are registered but NOT enqueued globally"
    - "Elementor conditionally loads assets only when widget is present on page"
  artifacts:
    - path: "devsroom-drilldown-mobile-menu.php"
      provides: "Plugin entry point with header, autoloader, plugins_loaded init"
      contains: "Plugin Name: Devsroom DrillDown Mobile Menu"
    - path: "src/Plugin.php"
      provides: "Singleton plugin class with Elementor check and hook registration"
      contains: "class Plugin"
      exports: ["get_instance", "init", "registerCategory", "registerWidget"]
    - path: "src/Admin/ElementorNotice.php"
      provides: "Admin notice when Elementor is inactive"
      contains: "class ElementorNotice"
    - path: "src/Assets/Registrar.php"
      provides: "Script/style registration (not enqueue)"
      contains: "wp_register_script"
    - path: "src/Elementor/Widget/DrillDownMenu.php"
      provides: "Elementor widget class with identity methods and dependency declarations"
      contains: "class DrillDownMenu"
      exports: ["get_name", "get_title", "get_icon", "get_categories", "get_script_depends", "get_style_depends"]
    - path: "assets/js/ddmm-frontend.js"
      provides: "Empty IIFE shell for Phase 5"
      min_lines: 8
    - path: "assets/css/ddmm-frontend.css"
      provides: "CSS custom properties skeleton"
      contains: "--ddmm-"
  key_links:
    - from: "devsroom-drilldown-mobile-menu.php"
      to: "src/Plugin.php"
      via: "PSR-4 autoloader (Devsroom_DDMM\\ namespace)"
      pattern: "spl_autoload_register"
    - from: "src/Plugin.php"
      to: "src/Elementor/Widget/DrillDownMenu.php"
      via: "elementor/widgets/register hook"
      pattern: "\\$widgets_manager->register"
    - from: "src/Plugin.php"
      to: "src/Assets/Registrar.php"
      via: "Direct instantiation in init()"
      pattern: "Registrar.*register"
    - from: "src/Elementor/Widget/DrillDownMenu.php"
      to: "assets/js/ddmm-frontend.js"
      via: "get_script_depends() returns 'ddmm-frontend'"
      pattern: "get_script_depends"
    - from: "src/Elementor/Widget/DrillDownMenu.php"
      to: "assets/css/ddmm-frontend.css"
      via: "get_style_depends() returns 'ddmm-frontend'"
      pattern: "get_style_depends"
---

<objective>
Create the complete plugin bootstrap: entry point with PSR-4 autoloader, Plugin singleton with Elementor dependency check, admin notice, widget registration with custom category, conditional asset loading, and skeleton CSS/JS files.

Purpose: This is the entire plugin foundation. Without this, no other phase can proceed. The widget must appear in the Elementor editor with a custom icon under a "Devsroom" category, and assets must only load when the widget is present on a page.

Output: 7 files forming the complete plugin skeleton — main entry point, Plugin class, admin notice, asset registrar, widget class, empty IIFE JS shell, and CSS custom properties skeleton.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-plugin-foundation-widget-shell/01-CONTEXT.md
@.planning/phases/01-plugin-foundation-widget-shell/01-RESEARCH.md

<interfaces>
<!-- No existing code interfaces — greenfield project. The contracts below are what this plan CREATES for downstream plans. -->

This plan creates the following interfaces consumed by Plan 02:

From src/Elementor/Widget/DrillDownMenu.php:
```php
// Widget extends \Elementor\Widget_Base
// Identity methods (already implemented in this plan)
public function get_name(): string;        // Returns 'ddmm-drilldown-menu'
public function get_title(): string;       // Returns 'DrillDown Mobile Menu'
public function get_icon(): string;        // Returns SVG data URI
public function get_categories(): array;   // Returns ['devsroom']
public function get_script_depends(): array;  // Returns ['ddmm-frontend']
public function get_style_depends(): array;   // Returns ['ddmm-frontend']

// Methods Plan 02 will add to:
protected function _register_controls(): void;  // Empty in this plan, populated in Plan 02
protected function render(): void;              // Empty wrapper in this plan, populated in Plan 02
```

From src/Plugin.php:
```php
namespace Devsroom_DDMM;

final class Plugin {
    private static ?self $instance = null;
    public static function get_instance(): self;
    private function __construct();
    public function init(): void;
    public function registerCategory(\Elementor\Elements_Manager $elements_manager): void;
    public function registerWidget(\Elementor\Widgets_Manager $widgets_manager): void;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Plugin Bootstrap, Autoloader, Admin Notice, and Asset Registrar</name>
  <files>
    devsroom-drilldown-mobile-menu.php
    src/Plugin.php
    src/Admin/ElementorNotice.php
    src/Assets/Registrar.php
    assets/js/ddmm-frontend.js
    assets/css/ddmm-frontend.css
  </files>
  <read_first>
    .planning/phases/01-plugin-foundation-widget-shell/01-CONTEXT.md (D-04, D-05, D-06, D-07, D-13, D-15, D-16 decisions)
    .planning/phases/01-plugin-foundation-widget-shell/01-RESEARCH.md (Pattern 1, Pattern 2, Pattern 6, Pattern 7, Anti-Patterns section, Pitfall 5)
    .planning/research/ARCHITECTURE.md (Recommended Project Structure, Pattern 2, Pattern 3, Anti-Pattern 5)
    .planning/research/PITFALLS.md (Pitfall 1, Pitfall 5, Pitfall 7, Integration Gotchas)
  </read_first>
  <action>
    Create the plugin entry point, bootstrap classes, and skeleton asset files.

    **1. Create `devsroom-drilldown-mobile-menu.php`** (per D-04, D-05, D-07):
    - WordPress plugin header with these exact values:
      - Plugin Name: Devsroom DrillDown Mobile Menu
      - Description: A mobile drill-down menu widget for Elementor with off-canvas drawer navigation.
      - Version: 0.0.01
      - Author: MEHEDI HASSAN SHUBHO
      - Text Domain: devsroom-drilldown-mobile-menu
      - Requires at least: 6.5
      - Requires PHP: 8.1
    - `if ( ! defined( 'ABSPATH' ) ) { exit; }` guard
    - PSR-4 autoloader via `spl_autoload_register()` (per D-04):
      - Prefix: `Devsroom_DDMM\\`
      - Base directory: `__DIR__ . '/src/'`
      - Map relative class name to file path: `str_replace( '\\', '/', $relative_class ) . '.php'`
      - Check `file_exists()` before requiring
    - Hook `plugins_loaded` to call `\Devsroom_DDMM\Plugin::get_instance()->init()`
    - Do NOT check for Elementor here — that is Plugin.php's responsibility

    **2. Create `src/Plugin.php`** (per D-05):
    - Namespace: `Devsroom_DDMM`
    - `use` statements for `Devsroom_DDMM\Admin\ElementorNotice`, `Devsroom_DDMM\Assets\Registrar`, `Devsroom_DDMM\Elementor\Widget\DrillDownMenu`
    - `final class Plugin` with singleton pattern:
      - `private static ?self $instance = null`
      - `public static function get_instance(): self` — creates instance if null
      - `private function __construct()` — empty, prevents external instantiation
      - `private function __clone()` — prevents cloning
    - `public function init(): void`:
      - Check `did_action( 'elementor/loaded' )` — if false, register admin notice and RETURN early (do not register widget or assets)
      - If Elementor is active:
        - Register admin notice anyway (the notice class itself checks `did_action('elementor/loaded')` internally, so it won't show when Elementor IS active)
        - `add_action( 'elementor/elements/categories_registered', [ $this, 'registerCategory' ] )` (per D-02)
        - `add_action( 'elementor/widgets/register', [ $this, 'registerWidget' ] )` (per WIDG-01)
        - Instantiate `Registrar` and call `register()` (per D-13)
    - `public function registerCategory( \Elementor\Elements_Manager $elements_manager ): void`:
      - `$elements_manager->add_category( 'devsroom', [ 'title' => esc_html__( 'Devsroom', 'devsroom-drilldown-mobile-menu' ) ] )`
    - `public function registerWidget( \Elementor\Widgets_Manager $widgets_manager ): void`:
      - `$widgets_manager->register( new DrillDownMenu() )`

    **3. Create `src/Admin/ElementorNotice.php`** (per D-06):
    - Namespace: `Devsroom_DDMM\Admin`
    - `class ElementorNotice`
    - `public function register(): void` — hooks `admin_notices` only if `! did_action( 'elementor/loaded' )`
    - `public function renderNotice(): void`:
      - Guard: `if ( ! current_user_can( 'activate_plugins' ) ) { return; }`
      - Check if Elementor plugin file exists at `WP_PLUGIN_DIR . '/elementor/elementor.php'`
      - If installed but inactive: generate activation URL with `wp_nonce_url( admin_url( 'plugins.php?action=activate&plugin=elementor/elementor.php' ), 'activate-plugin_elementor/elementor.php' )`
      - If not installed: use `admin_url( 'plugin-install.php?s=elementor&tab=search&type=term' )`
      - Show `notice notice-warning is-dismissible` div with the appropriate link
      - All strings wrapped with `esc_html__()` using text domain `devsroom-drilldown-mobile-menu`
      - Use `wp_kses_post()` for the message containing HTML link

    **4. Create `src/Assets/Registrar.php`** (per D-13, D-14):
    - Namespace: `Devsroom_DDMM\Assets`
    - `class Registrar`
    - `public function register(): void`:
      - `add_action( 'wp_enqueue_scripts', function (): void { ... } )`
      - Inside the callback:
        - `wp_register_script( 'ddmm-frontend', plugins_url( 'assets/js/ddmm-frontend.js', dirname( __DIR__, 2 ) ), [], '0.0.01', true )`
        - `wp_register_style( 'ddmm-frontend', plugins_url( 'assets/css/ddmm-frontend.css', dirname( __DIR__, 2 ) ), [], '0.0.01' )`
      - REGISTER ONLY — never call `wp_enqueue_script()` or `wp_enqueue_style()` (per Anti-Pattern 5 in RESEARCH.md)

    **5. Create `assets/js/ddmm-frontend.js`** (per D-15):
    - Empty IIFE shell:
      ```javascript
      /**
       * Devsroom DrillDown Mobile Menu - Frontend
       *
       * Phase 1: Shell only. Populated in Phase 5 with drill-down logic.
       */
      ( function() {
          'use strict';

          // Drill-down menu logic will be added in Phase 5.
      } )();
      ```

    **6. Create `assets/css/ddmm-frontend.css`** (per D-16):
    - File header comment: "Devsroom DrillDown Mobile Menu — Phase 1: CSS Custom Properties Skeleton"
    - `.elementor-widget-ddmm-drilldown-menu` root selector containing CSS custom properties:
      ```css
      --ddmm-trigger-color: #1a1a1a;
      --ddmm-trigger-bg: transparent;
      --ddmm-trigger-padding: 8px;
      --ddmm-trigger-border: none;
      --ddmm-trigger-border-radius: 0;
      --ddmm-trigger-cursor: pointer;
      --ddmm-trigger-font-size: 16px;
      --ddmm-hamburger-width: 28px;
      --ddmm-hamburger-height: 20px;
      --ddmm-hamburger-line-height: 2px;
      --ddmm-hamburger-line-gap: 6px;
      --ddmm-hamburger-line-color: var(--ddmm-trigger-color);
      --ddmm-hamburger-line-radius: 1px;
      --ddmm-transition-duration: 300ms;
      ```
    - No other CSS rules in this file — trigger button styles come in Plan 02

    **CRITICAL rules to follow:**
    - Directory casing MUST match namespace casing: `src/Admin/`, `src/Assets/`, `src/Elementor/Widget/` (per Pitfall 1 in RESEARCH.md about Linux case-sensitivity)
    - Use `plugins_url()` with `dirname( __DIR__, 2 )` from Registrar.php to resolve to plugin root URL, OR use `plugin_dir_url( dirname( __DIR__, 2 ) . '/devsroom-drilldown-mobile-menu.php' )` — verify the path resolves correctly
    - Every `echo` must use escaping functions (per PLUG-06)
    - All translatable strings must use `esc_html__()` with text domain `devsroom-drilldown-mobile-menu`
  </action>
  <verify>
    <automated>cd "D:\Devsroom-Work\Plugins\devsroom-drilldown-mobile-menu" && php -l devsroom-drilldown-mobile-menu.php && php -l src/Plugin.php && php -l src/Admin/ElementorNotice.php && php -l src/Assets/Registrar.php && echo "PHP_SYNTAX_OK"</automated>
  </verify>
  <acceptance_criteria>
    - `devsroom-drilldown-mobile-menu.php` contains `Plugin Name: Devsroom DrillDown Mobile Menu`
    - `devsroom-drilldown-mobile-menu.php` contains `Version: 0.0.01`
    - `devsroom-drilldown-mobile-menu.php` contains `Author: MEHEDI HASSAN SHUBHO`
    - `devsroom-drilldown-mobile-menu.php` contains `Text Domain: devsroom-drilldown-mobile-menu`
    - `devsroom-drilldown-mobile-menu.php` contains `Requires at least: 6.5`
    - `devsroom-drilldown-mobile-menu.php` contains `Requires PHP: 8.1`
    - `devsroom-drilldown-mobile-menu.php` contains `spl_autoload_register`
    - `devsroom-drilldown-mobile-menu.php` contains `Devsroom_DDMM\\Plugin::get_instance()->init()`
    - `devsroom-drilldown-mobile-menu.php` does NOT contain `wp_enqueue_script` or `wp_enqueue_style`
    - `src/Plugin.php` contains `final class Plugin`
    - `src/Plugin.php` contains `did_action( 'elementor/loaded' )`
    - `src/Plugin.php` contains `elementor/elements/categories_registered`
    - `src/Plugin.php` contains `elementor/widgets/register`
    - `src/Plugin.php` contains `$widgets_manager->register( new DrillDownMenu() )`
    - `src/Admin/ElementorNotice.php` contains `class ElementorNotice`
    - `src/Admin/ElementorNotice.php` contains `did_action( 'elementor/loaded' )`
    - `src/Admin/ElementorNotice.php` contains `current_user_can( 'activate_plugins' )`
    - `src/Admin/ElementorNotice.php` contains `notice notice-warning is-dismissible`
    - `src/Assets/Registrar.php` contains `wp_register_script`
    - `src/Assets/Registrar.php` contains `wp_register_style`
    - `src/Assets/Registrar.php` does NOT contain `wp_enqueue_script` or `wp_enqueue_style`
    - `assets/js/ddmm-frontend.js` contains `( function()`
    - `assets/js/ddmm-frontend.js` contains `'use strict'`
    - `assets/css/ddmm-frontend.css` contains `--ddmm-trigger-color`
    - `assets/css/ddmm-frontend.css` contains `--ddmm-transition-duration`
    - `assets/css/ddmm-frontend.css` contains `.elementor-widget-ddmm-drilldown-menu`
    - All 4 PHP files pass `php -l` syntax check
  </acceptance_criteria>
  <done>
    Plugin entry point exists with correct header, PSR-4 autoloader, and plugins_loaded init. Plugin singleton checks Elementor presence, shows admin notice if missing, registers "Devsroom" category and widget if present. Asset registrar registers (not enqueues) script and style handles. JS file is empty IIFE shell. CSS file has --ddmm-* custom properties skeleton. All PHP files pass syntax check.
  </done>
</task>

<task type="auto">
  <name>Task 2: Elementor Widget Class with Identity, Icon, and Dependency Declarations</name>
  <files>
    src/Elementor/Widget/DrillDownMenu.php
  </files>
  <read_first>
    .planning/phases/01-plugin-foundation-widget-shell/01-CONTEXT.md (D-01, D-02, D-03, D-14 decisions)
    .planning/phases/01-plugin-foundation-widget-shell/01-RESEARCH.md (Pattern 4, Pattern 5, Widget Icon section, Anti-Patterns section)
    .planning/research/ARCHITECTURE.md (Pattern 1, Pattern 3, Anti-Pattern 5)
    .planning/research/PITFALLS.md (Pitfall 5, Pitfall 7, Integration Gotchas: get_style_depends)
    src/Plugin.php (created in Task 1 — verify the registration call matches)
  </read_first>
  <action>
    Create the Elementor widget class with all identity methods, inline SVG icon, category assignment, and asset dependency declarations.

    **Create `src/Elementor/Widget/DrillDownMenu.php`** (per D-01, D-02, D-03, D-14):

    - Namespace: `Devsroom_DDMM\Elementor\Widget`
    - `class DrillDownMenu extends \Elementor\Widget_Base`
    - Implement these methods:

    **`get_name(): string`** (per D-01):
    - Return `'ddmm-drilldown-menu'` (exact string)

    **`get_title(): string`**:
    - Return `esc_html__( 'DrillDown Mobile Menu', 'devsroom-drilldown-mobile-menu' )`

    **`get_icon(): string`** (per D-03):
    - Return SVG data URI of a hamburger icon (three horizontal lines):
      ```php
      return 'data:image/svg+xml;base64,' . base64_encode(
          '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"><line x1="3" y1="6" x2="21" y2="6"/><line x1="3" y1="12" x2="21" y2="12"/><line x1="3" y1="18" x2="21" y2="18"/></svg>'
      );
      ```
    - If this does not display correctly in Elementor, the fallback is the string `'eicon-menu-bar'`

    **`get_categories(): array`** (per D-02):
    - Return `['devsroom']` — this matches the category slug registered in Plugin.php

    **`get_script_depends(): array`** (per D-14, WIDG-04, PLUG-05):
    - Return `['ddmm-frontend']` — this handle must match the one registered in Registrar.php
    - Do NOT access `$this->get_settings()` here — it does not work in editor mode (per Pitfall 2 in RESEARCH.md)

    **`get_style_depends(): array`** (per D-14, WIDG-04, PLUG-05):
    - Return `['ddmm-frontend']` — same handle as the style registered in Registrar.php

    **`_register_controls(): void`**:
    - Leave as empty method body with a comment: `// Content Tab controls added in Plan 02 / Phase 1.`

    **`render(): void`**:
    - Leave as empty method body with a comment: `// Trigger button rendering added in Plan 02 / Phase 1.`
    - This means the widget will render as empty in the Elementor editor until Plan 02 adds trigger button rendering

    **Key constraints:**
    - Do NOT use `$this->get_settings()` inside `get_script_depends()` or `get_style_depends()` — it does not work in editor mode (per RESEARCH.md Pitfall 2)
    - The widget class must be instantiable without constructor arguments — `new DrillDownMenu()` must work (per Plugin.php registration call)
    - No jQuery dependency anywhere (per COMP-02 and project constraint)
  </action>
  <verify>
    <automated>cd "D:\Devsroom-Work\Plugins\devsroom-drilldown-mobile-menu" && php -l src/Elementor/Widget/DrillDownMenu.php && echo "PHP_SYNTAX_OK"</automated>
  </verify>
  <acceptance_criteria>
    - `src/Elementor/Widget/DrillDownMenu.php` contains `class DrillDownMenu extends \\Elementor\\Widget_Base`
    - `src/Elementor/Widget/DrillDownMenu.php` contains `return 'ddmm-drilldown-menu'` in get_name()
    - `src/Elementor/Widget/DrillDownMenu.php` contains `return ['devsroom']` in get_categories()
    - `src/Elementor/Widget/DrillDownMenu.php` contains `return ['ddmm-frontend']` in get_script_depends()
    - `src/Elementor/Widget/DrillDownMenu.php` contains `return ['ddmm-frontend']` in get_style_depends()
    - `src/Elementor/Widget/DrillDownMenu.php` contains `data:image/svg+xml;base64` in get_icon()
    - `src/Elementor/Widget/DrillDownMenu.php` does NOT contain `wp_enqueue_script` or `wp_enqueue_style`
    - `src/Elementor/Widget/DrillDownMenu.php` does NOT contain `$this->get_settings` inside get_script_depends or get_style_depends
    - File passes `php -l` syntax check
    - Directory path is exactly `src/Elementor/Widget/DrillDownMenu.php` (case-sensitive for Linux)
  </acceptance_criteria>
  <done>
    Widget class exists extending Widget_Base with correct identity (name ddmm-drilldown-menu, custom SVG icon, Devsroom category). Asset dependency declarations return 'ddmm-frontend' handles. Empty _register_controls() and render() stubs ready for Plan 02. File passes PHP syntax check.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin dashboard | Admin notice HTML output — must escape all dynamic values |
| WordPress plugin header | Static values — no user input |
| Elementor editor | Widget registration — no user input processed yet |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | S (Spoofing) | Admin notice | accept | Admin notice is informational only, no authentication bypass possible |
| T-01-02 | X (Elevation) | Widget registration | accept | Registration requires Elementor admin context; no user-facing input processed |
| T-01-03 | I (Information Disclosure) | Asset URLs | accept | Plugin directory URLs are public by design in WordPress |
| T-01-04 | X (Elevation) | Admin notice activation link | mitigate | Use wp_nonce_url() for activation link to prevent CSRF |
</threat_model>

<verification>
1. All PHP files pass `php -l` syntax check
2. Directory structure matches PSR-4 namespace mapping exactly (case-sensitive)
3. `devsroom-drilldown-mobile-menu.php` has complete plugin header
4. No `wp_enqueue_script()` or `wp_enqueue_style()` calls anywhere — only `wp_register_*()`
5. Widget class returns correct handles from `get_script_depends()` and `get_style_depends()`
</verification>

<success_criteria>
- Plugin file exists with correct WordPress plugin header declaring all identity fields (D-07)
- PSR-4 autoloader maps Devsroom_DDMM\ namespace to src/ directory (D-04)
- Plugin singleton initializes on plugins_loaded, checks Elementor, shows admin notice if missing (D-05, D-06)
- Widget class extends Widget_Base with name 'ddmm-drilldown-menu', custom SVG icon, in 'devsroom' category (D-01, D-02, D-03)
- Asset registration uses wp_register_* only — get_script_depends/get_style_depends declare handles (D-13, D-14)
- JS file is empty IIFE shell (D-15)
- CSS file has --ddmm-* custom properties skeleton (D-16)
- All PHP files pass syntax validation
- Zero jQuery dependency
- Zero wp_enqueue calls
</success_criteria>

<output>
After completion, create `.planning/phases/01-plugin-foundation-widget-shell/01-01-SUMMARY.md`
</output>

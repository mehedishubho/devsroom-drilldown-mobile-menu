---
phase: 06-style-tab-controls
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/Elementor/Widget/DrillDownMenu.php
  - assets/css/ddmm-frontend.css
autonomous: true
requirements: [STYL-01, STYL-02, STYL-03]
requirements_decision_coverage: [D-01, D-02, D-03, D-05, D-06]

must_haves:
  truths:
    - "User can style the Trigger Button (color, background, hamburger size, padding, border, typography) with Normal/Hover tabs"
    - "User can style the Drawer (responsive width in px/vw/%, background, box-shadow, overlay color)"
    - "User can style the Header (background, border, height, title typography/color, close button color)"
    - "Trigger and Header hover states apply via :hover selectors"
    - "Polished baseline (D-01) makes the un-styled widget look designed on first drop-in"
  artifacts:
    - path: "src/Elementor/Widget/DrillDownMenu.php"
      provides: "Three TAB_STYLE sections (section_style_trigger, section_style_drawer, section_style_header) appended to _register_controls()"
      contains: "section_style_trigger"
    - path: "assets/css/ddmm-frontend.css"
      provides: "Refined baseline --ddmm-* defaults (D-01): softer menu-border-color, drawer box-shadow var, refined header padding; plus new --ddmm-* hooks for trigger/header/drawer style properties not yet covered"
      contains: "--ddmm-drawer-box-shadow"
  key_links:
    - from: "DrillDownMenu.php section_style_trigger controls"
      to: ".ddmm-trigger / .ddmm-hamburger / .ddmm-trigger__text BEM classes"
      via: "Elementor selectors arrays using {{WRAPPER}} token"
      pattern: "\\{\\{WRAPPER\\}\\} .ddmm-trigger"
    - from: "DrillDownMenu.php section_style_drawer controls"
      to: ".ddmm-drawer / .ddmm-overlay BEM classes + --ddmm-drawer-* / --ddmm-overlay-bg vars"
      via: "Elementor selectors arrays"
      pattern: "--ddmm-drawer-width: \\{\\{SIZE\\}\\}\\{\\{UNIT\\}\\}"
---

<objective>
Add the first three Style Tab sections (Trigger STYL-01, Drawer STYL-02, Header STYL-03) to the Elementor widget, and refine the baseline CSS defaults per D-01 (polish-the-baseline) so the widget looks designed on first drop-in.

Purpose: Deliver the first half of the Phase 6 Style Tab so users can fully customize trigger, drawer, and header. The refined baseline (D-01) is the foundation the new Style Tab `default` values must match (Pitfall 9: no drift between CSS var defaults and control defaults).

Output: Three new `TAB_STYLE` sections in `_register_controls()`; refined `--ddmm-*` baseline values in `ddmm-frontend.css`; new `--ddmm-*` hooks (drawer box-shadow, header border color, trigger hover bg, etc.) so Style Tab controls can drive properties not yet on a var.

This plan MUST complete before 06-02 (which appends to the same `_register_controls()` method) and before 06-03 (which verifies the editor-preview parity against these selectors).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-style-tab-controls/06-CONTEXT.md
@.planning/phases/06-style-tab-controls/06-RESEARCH.md
@.planning/phases/06-style-tab-controls/06-VALIDATION.md

@src/Elementor/Widget/DrillDownMenu.php
@assets/css/ddmm-frontend.css

<interfaces>
<!-- Existing var declarations on .elementor-widget-ddmm-drilldown-menu (ddmm-frontend.css lines 8-36) the new controls MUST match defaults against: -->

```css
/* Trigger */
--ddmm-trigger-color: #1a1a1a;
--ddmm-trigger-bg: transparent;
--ddmm-trigger-padding: 8px;
--ddmm-trigger-border: none;
--ddmm-trigger-border-radius: 0;
--ddmm-trigger-font-size: 16px;
--ddmm-hamburger-width: 28px;
--ddmm-hamburger-height: 20px;
--ddmm-hamburger-line-height: 2px;
--ddmm-hamburger-line-gap: 6px;
--ddmm-hamburger-line-color: var(--ddmm-trigger-color);
--ddmm-hamburger-line-radius: 1px;

/* Drawer + Layout */
--ddmm-drawer-width: 320px;
--ddmm-drawer-bg: #ffffff;
--ddmm-overlay-bg: rgba(0, 0, 0, 0.5);
--ddmm-header-height: 56px;
--ddmm-brand-max-height: 40px;
--ddmm-menu-border-color: rgba(0, 0, 0, 0.05);
```

<!-- Existing BEM classes in ddmm-frontend.css that Style Tab selectors target (DO NOT rename): -->
- `.ddmm-trigger`, `.ddmm-hamburger`, `.ddmm-hamburger__line`, `.ddmm-trigger__text`
- `.ddmm-overlay`, `.ddmm-drawer`, `.ddmm-header`, `.ddmm-brand__text`, `.ddmm-brand__img`, `.ddmm-close`

<!-- Elementor API contracts (from 06-RESEARCH.md, all HIGH confidence): -->
- `start_controls_section($id, ['label' => ..., 'tab' => \Elementor\Controls_Manager::TAB_STYLE])` ... `end_controls_section()`
- Individual controls use `'selectors' => [ '{{WRAPPER}} .ddmm-<bem>' => 'prop: {{VALUE}};' ]` (PLURAL selectors)
- Group controls use singular `'selector' => '{{WRAPPER}} .ddmm-<bem>'` (NOT selectors — Pitfall: silent fail)
- DIMENSIONS tokens: `{{TOP}}{{UNIT}} {{RIGHT}}{{UNIT}} {{BOTTOM}}{{UNIT}} {{LEFT}}{{UNIT}}` (one UNIT per number — Pitfall 6)
- Responsive: `'responsive' => true` on SLIDER/DIMENSIONS only (never COLOR — Pitfall: anti-pattern)
- Active state (NOT in this plan — that's 06-02): marker-class selectors, NOT States API
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Refine baseline CSS defaults (D-01) and add new --ddmm-* hooks for trigger/drawer/header Style Tab properties</name>
  <read_first>
    - .planning/phases/06-style-tab-controls/06-RESEARCH.md (§ Architecture Patterns 1 + Pitfalls 2/8/9; § Open Questions #4 refined-baseline recommendation)
    - .planning/phases/06-style-tab-controls/06-CONTEXT.md (D-01, D-05, D-06, "Carried Forward — Locked")
    - assets/css/ddmm-frontend.css (the full file — focus on the `:root`-style var block lines 8-36 and the `.ddmm-trigger` / `.ddmm-drawer` / `.ddmm-header` rule blocks)
    - src/Elementor/Widget/DrillDownMenu.php (the existing `_register_controls()` pattern that Task 2 will extend; the `render()` block lines 557-672 that emits `.ddmm-widget` + inline `--ddmm-transition-*` so you see the var-bridge wiring)
  </read_first>
  <files>assets/css/ddmm-frontend.css</files>
  <action>
Refine the baseline CSS defaults per D-01 (polish-the-baseline) and add new `--ddmm-*` custom-property hooks so Task 2's Style Tab controls can drive every trigger/drawer/header property without specificity wars (Pitfall 2). All edits happen in `assets/css/ddmm-frontend.css` only.

**A. Refine existing var defaults (D-01 — softer borders, refined spacing, subtle drawer shadow) inside the `.elementor-widget-ddmm-drilldown-menu { … }` block (lines 8-36).** Change these EXACT values:
- `--ddmm-menu-border-color: rgba(0, 0, 0, 0.05);` → `--ddmm-menu-border-color: rgba(0, 0, 0, 0.06);` (softer)
- Add a NEW drawer box-shadow var: `--ddmm-drawer-box-shadow: 0 6px 24px rgba(0, 0, 0, 0.12);` (subtle, designed)
- Add a NEW header border-bottom color var: `--ddmm-header-border-color: rgba(0, 0, 0, 0.06);`
- Add a NEW trigger hover-background var: `--ddmm-trigger-hover-bg: rgba(0, 0, 0, 0.04);`
- Add a NEW trigger hover-color var: `--ddmm-trigger-hover-color: var(--ddmm-trigger-color);`
- Add a NEW close-button color var: `--ddmm-close-color: inherit;`
- Add a NEW close-button hover-color var: `--ddmm-close-hover-color: var(--ddmm-trigger-color);`
- Add a NEW header title color var: `--ddmm-header-title-color: #1a1a1a;`

**B. Consume the new vars in the existing rule blocks (so the refined baseline ships even before the Style Tab controls register):**
- In `.ddmm-drawer { … }` (lines 112-127): add `box-shadow: var(--ddmm-drawer-box-shadow);` inside the rule.
- In `.ddmm-header { … }` (lines 130-148): add `border-bottom: 1px solid var(--ddmm-header-border-color);` to the rule; change `.ddmm-brand__text { font-size: 18px; font-weight: 600; }` to `.ddmm-brand__text { font-size: 18px; font-weight: 600; color: var(--ddmm-header-title-color); }`.
- In `.ddmm-close { … }` (lines 151-160): add `color: var(--ddmm-close-color);` (replacing the existing `color: inherit;`).
- In `.ddmm-trigger { … }` (lines 43-59): add `transition: background var(--ddmm-transition-duration) var(--ddmm-transition-easing), color var(--ddmm-transition-duration) var(--ddmm-transition-easing);` (already present — keep it); immediately AFTER the `.ddmm-trigger` rule add a NEW rule:
  ```css
  .ddmm-trigger:hover {
      background: var(--ddmm-trigger-hover-bg);
      color: var(--ddmm-trigger-hover-color);
  }
  ```
- Immediately AFTER the `.ddmm-close` rule add a NEW rule:
  ```css
  .ddmm-close:hover {
      color: var(--ddmm-close-hover-color);
  }
  ```

**C. Pitfall 2 audit (specificity safety — D-04 Active marker not yet styled by this plan but ensure the var-driven Phase 5 rule stays safe):** The existing `.ddmm-menu__item.ddmm-current-item > a { font-weight: 600; color: var(--ddmm-trigger-color); }` (lines 508-514) is already var-driven and !important-free (research A2 verified). DO NOT modify it in this plan. Leave the lines untouched. (Plan 06-02 owns the Active state styling.)

**D. Pitfall 8 prep:** The existing `.ddmm-editor-preview .ddmm-menu__item { background: #fff; border-bottom: 1px solid #eee; }` block (lines 284-287) is the hardcoded-color target that Plan 06-03 Task 2 removes. DO NOT touch it in this plan — leave it for 06-03 to clean up so the editor preview doesn't visually regress before 06-03 ships its neutralizers.

Use CSS native nesting (the file already uses it for `.ddmm-header & .ddmm-brand__text`). Match the existing indentation style (tabs, one property per line).
  </action>
  <verify>
    <automated>php --version &amp;&amp; grep -c "ddmm-drawer-box-shadow" assets/css/ddmm-frontend.css</automated>
  </verify>
  <acceptance_criteria>
- `grep -cE "\-\-ddmm-drawer-box-shadow" assets/css/ddmm-frontend.css` returns ≥ 2 (one declaration, one usage)
- `grep -cE "\-\-ddmm-header-border-color|\-\-ddmm-header-title-color|\-\-ddmm-close-color|\-\-ddmm-close-hover-color|\-\-ddmm-trigger-hover-bg|\-\-ddmm-trigger-hover-color" assets/css/ddmm-frontend.css` returns ≥ 6 (each declared once) AND ≥ 12 total when usage lines are counted
- `grep -cE "\.ddmm-trigger:hover" assets/css/ddmm-frontend.css` returns ≥ 1
- `grep -cE "\.ddmm-close:hover" assets/css/ddmm-frontend.css` returns ≥ 1
- `grep -cE "\-\-ddmm-menu-border-color: rgba\(0, 0, 0, 0.06\)" assets/css/ddmm-frontend.css` returns ≥ 1
- The Phase 5 `.ddmm-menu__item.ddmm-current-item > a` rule remains UNTOUCHED (visually verify by reading lines 508-514)
- The `.ddmm-editor-preview .ddmm-menu__item { background: #fff … }` rule remains UNTOUCHED (visually verify by reading lines 284-287)
  </acceptance_criteria>
  <done>
CSS baseline refined per D-01. Six new `--ddmm-*` hooks declared and consumed. Trigger/Close hover rules added. Phase 5 active-marker rule untouched (Pitfall 2 safe). Editor-preview hardcoded-color removal is deferred to 06-03 as planned.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add three Style Tab sections (Trigger STYL-01, Drawer STYL-02, Header STYL-03) to _register_controls()</name>
  <read_first>
    - .planning/phases/06-style-tab-controls/06-RESEARCH.md (§ Architecture Patterns 1/2/4/5 + § Code Examples A/B + § Pitfalls 1/3/5/6/7 + § Anti-Patterns — REQUIRED, the exact control shapes live here)
    - .planning/phases/06-style-tab-controls/06-CONTEXT.md (D-01, D-02 trigger text typography, D-03 trigger hover, D-05 drawer width responsive, D-06 padding dimensions)
    - .planning/phases/06-style-tab-controls/06-VALIDATION.md (the STYL-01/02/03 grep checks ARE the acceptance criteria — copy them exactly)
    - src/Elementor/Widget/DrillDownMenu.php (the existing `_register_controls()` method — the new sections append AFTER `end_controls_section()` for `section_search`, line 520)
    - assets/css/ddmm-frontend.css (the refined var block from Task 1 — control `default` values MUST match the refined var values per Pitfall 9)
  </read_first>
  <files>src/Elementor/Widget/DrillDownMenu.php</files>
  <action>
Append three `TAB_STYLE` sections to `_register_controls()`, AFTER the existing `end_controls_section()` for the Content Tab `section_search` (line 520) and BEFORE the method's closing brace (line 521). Use the established pattern: `start_controls_section` → controls/group controls (wrapped in `start_controls_tabs` for state variants) → `end_controls_section`.

Every control MUST use `\Elementor\Controls_Manager::<CONST>` (fully-qualified — match the existing Content Tab controls). Every user-facing string MUST be wrapped in `esc_html__( '…', 'devsroom-drilldown-mobile-menu' )`. Group control `name` keys MUST be unique across the whole widget (Pitfall 3).

**SECTION 1: `section_style_trigger` (STYL-01 + D-02 trigger text typography + D-03 trigger hover)**

```php
$this->start_controls_section(
    'section_style_trigger',
    [
        'label'     => esc_html__( 'Trigger Button', 'devsroom-drilldown-mobile-menu' ),
        'tab'       => \Elementor\Controls_Manager::TAB_STYLE,
        'separator' => 'before',   // separate from the last Content Tab section (Pitfall 5)
    ]
);
```

Inside, emit in this order:

1. **Normal/Hover state tabs (D-03):**
```php
$this->start_controls_tabs( 'trigger_state_tabs' );

// NORMAL
$this->start_controls_tab( 'trigger_normal', [ 'label' => esc_html__( 'Normal', 'devsroom-drilldown-mobile-menu' ) ] );
$this->add_control( 'trigger_color', [
    'label'     => esc_html__( 'Color', 'devsroom-drilldown-mobile-menu' ),
    'type'      => \Elementor\Controls_Manager::COLOR,
    'default'   => '#1a1a1a',   // matches --ddmm-trigger-color (Pitfall 9)
    'selectors' => [ '{{WRAPPER}}' => '--ddmm-trigger-color: {{VALUE}};' ],
] );
$this->add_control( 'trigger_bg', [
    'label'     => esc_html__( 'Background', 'devsroom-drilldown-mobile-menu' ),
    'type'      => \Elementor\Controls_Manager::COLOR,
    'selectors' => [ '{{WRAPPER}}' => '--ddmm-trigger-bg: {{VALUE}};' ],
] );
$this->end_controls_tab();

// HOVER (D-03)
$this->start_controls_tab( 'trigger_hover', [ 'label' => esc_html__( 'Hover', 'devsroom-drilldown-mobile-menu' ) ] );
$this->add_control( 'trigger_hover_color', [
    'label'     => esc_html__( 'Color', 'devsroom-drilldown-mobile-menu' ),
    'type'      => \Elementor\Controls_Manager::COLOR,
    'selectors' => [ '{{WRAPPER}} .ddmm-trigger:hover' => 'color: {{VALUE}};' ],
] );
$this->add_control( 'trigger_hover_bg', [
    'label'     => esc_html__( 'Background', 'devsroom-drilldown-mobile-menu' ),
    'type'      => \Elementor\Controls_Manager::COLOR,
    'selectors' => [ '{{WRAPPER}} .ddmm-trigger:hover' => 'background: {{VALUE}};' ],
] );
$this->end_controls_tab();

$this->end_controls_tabs();
```

2. **Hamburger size (D-05 responsive):**
```php
$this->add_control( 'hamburger_width', [
    'label'      => esc_html__( 'Hamburger Size', 'devsroom-drilldown-mobile-menu' ),
    'type'       => \Elementor\Controls_Manager::SLIDER,
    'size_units' => [ 'px' ],
    'responsive' => true,   // D-05
    'range'      => [ 'px' => [ 'min' => 16, 'max' => 48, 'step' => 1 ] ],
    'default'    => [ 'unit' => 'px', 'size' => 28 ],   // matches --ddmm-hamburger-width
    'selectors'  => [ '{{WRAPPER}}' => '--ddmm-hamburger-width: {{SIZE}}{{UNIT}};' ],
] );
```

3. **Padding (D-06 per-side Dimensions, linked by default):**
```php
$this->add_control( 'trigger_padding', [
    'label'      => esc_html__( 'Padding', 'devsroom-drilldown-mobile-menu' ),
    'type'       => \Elementor\Controls_Manager::DIMENSIONS,
    'size_units' => [ 'px', 'em' ],
    'default'    => [ 'top' => 8, 'right' => 8, 'bottom' => 8, 'left' => 8, 'unit' => 'px', 'isLinked' => true ],
    'selectors'  => [ '{{WRAPPER}} .ddmm-trigger' => 'padding: {{TOP}}{{UNIT}} {{RIGHT}}{{UNIT}} {{BOTTOM}}{{UNIT}} {{LEFT}}{{UNIT}};' ],   // Pitfall 6
] );
```

4. **Border (group control — singular `selector`):**
```php
$this->add_group_control(
    \Elementor\Group_Control_Border::get_type(),
    [
        'name'     => 'trigger_border',
        'label'    => esc_html__( 'Border', 'devsroom-drilldown-mobile-menu' ),
        'selector' => '{{WRAPPER}} .ddmm-trigger',
    ]
);
```

5. **Typography (D-02 trigger text — applies to the trigger button + its text span):**
```php
$this->add_group_control(
    \Elementor\Group_Control_Typography::get_type(),
    [
        'name'     => 'trigger_text_typography',
        'label'    => esc_html__( 'Typography', 'devsroom-drilldown-mobile-menu' ),
        'selector' => '{{WRAPPER}} .ddmm-trigger, {{WRAPPER}} .ddmm-trigger__text',
    ]
);
```

Then `$this->end_controls_section();`

**SECTION 2: `section_style_drawer` (STYL-02 + D-05 drawer width responsive)**

```php
$this->start_controls_section(
    'section_style_drawer',
    [
        'label' => esc_html__( 'Drawer', 'devsroom-drilldown-mobile-menu' ),
        'tab'   => \Elementor\Controls_Manager::TAB_STYLE,
    ]
);
```

Inside, emit in this order:

1. **Width (D-05 responsive, px/vw/% per STYL-02):**
```php
$this->add_control( 'drawer_width', [
    'label'      => esc_html__( 'Drawer Width', 'devsroom-drilldown-mobile-menu' ),
    'type'       => \Elementor\Controls_Manager::SLIDER,
    'size_units' => [ 'px', 'vw', '%' ],   // STYL-02 requirement
    'responsive' => true,                   // D-05
    'range'      => [
        'px' => [ 'min' => 240, 'max' => 600, 'step' => 10 ],
        'vw' => [ 'min' => 50,  'max' => 100, 'step' => 1  ],
        '%'  => [ 'min' => 50,  'max' => 100, 'step' => 1  ],
    ],
    'default'    => [ 'unit' => 'px', 'size' => 320 ],   // matches --ddmm-drawer-width
    'selectors'  => [ '{{WRAPPER}}' => '--ddmm-drawer-width: {{SIZE}}{{UNIT}};' ],
    // NOTE: the CSS retains max-width:85vw as a safety net (D-05).
] );
```

2. **Background:**
```php
$this->add_control( 'drawer_bg', [
    'label'     => esc_html__( 'Drawer Background', 'devsroom-drilldown-mobile-menu' ),
    'type'      => \Elementor\Controls_Manager::COLOR,
    'default'   => '#ffffff',   // matches --ddmm-drawer-bg
    'selectors' => [ '{{WRAPPER}}' => '--ddmm-drawer-bg: {{VALUE}};' ],
] );
```

3. **Box Shadow (group control):**
```php
$this->add_group_control(
    \Elementor\Group_Control_Box_Shadow::get_type(),
    [
        'name'     => 'drawer_box_shadow',
        'label'    => esc_html__( 'Box Shadow', 'devsroom-drilldown-mobile-menu' ),
        'selector' => '{{WRAPPER}} .ddmm-drawer',
    ]
);
```

4. **Overlay Color (alpha-enabled per STYL-02):**
```php
$this->add_control( 'overlay_color', [
    'label'     => esc_html__( 'Overlay Color', 'devsroom-drilldown-mobile-menu' ),
    'type'      => \Elementor\Controls_Manager::COLOR,
    'alpha'     => true,   // overlay is semi-transparent by design
    'default'   => 'rgba(0,0,0,0.5)',   // matches --ddmm-overlay-bg
    'selectors' => [ '{{WRAPPER}}' => '--ddmm-overlay-bg: {{VALUE}};' ],
] );
```

Then `$this->end_controls_section();`

**SECTION 3: `section_style_header` (STYL-03 + D-02 title typography + D-03 close hover)**

```php
$this->start_controls_section(
    'section_style_header',
    [
        'label' => esc_html__( 'Header', 'devsroom-drilldown-mobile-menu' ),
        'tab'   => \Elementor\Controls_Manager::TAB_STYLE,
    ]
);
```

Inside, emit in this order:

1. **Header Background:**
```php
$this->add_control( 'header_bg', [
    'label'     => esc_html__( 'Background', 'devsroom-drilldown-mobile-menu' ),
    'type'      => \Elementor\Controls_Manager::COLOR,
    'selectors' => [ '{{WRAPPER}} .ddmm-header' => 'background: {{VALUE}};' ],
] );
```

2. **Header Border (group control):**
```php
$this->add_group_control(
    \Elementor\Group_Control_Border::get_type(),
    [
        'name'     => 'header_border',
        'label'    => esc_html__( 'Border', 'devsroom-drilldown-mobile-menu' ),
        'selector' => '{{WRAPPER}} .ddmm-header',
    ]
);
```

3. **Header Height (SLIDER, single-value — header height is not in the D-05 responsive list, but making it responsive is harmless; keep non-responsive for simplicity unless D-05 explicitly lists it — it does NOT, so omit `'responsive'`):**
```php
$this->add_control( 'header_height', [
    'label'      => esc_html__( 'Header Height', 'devsroom-drilldown-mobile-menu' ),
    'type'       => \Elementor\Controls_Manager::SLIDER,
    'size_units' => [ 'px' ],
    'range'      => [ 'px' => [ 'min' => 40, 'max' => 120, 'step' => 1 ] ],
    'default'    => [ 'unit' => 'px', 'size' => 56 ],   // matches --ddmm-header-height
    'selectors'  => [ '{{WRAPPER}}' => '--ddmm-header-height: {{SIZE}}{{UNIT}};' ],
] );
```

4. **Title Typography (D-02 — required by STYL-03):**
```php
$this->add_group_control(
    \Elementor\Group_Control_Typography::get_type(),
    [
        'name'     => 'header_title_typography',
        'label'    => esc_html__( 'Title Typography', 'devsroom-drilldown-mobile-menu' ),
        'selector' => '{{WRAPPER}} .ddmm-brand__text',
    ]
);
```

5. **Title Color:**
```php
$this->add_control( 'header_title_color', [
    'label'     => esc_html__( 'Title Color', 'devsroom-drilldown-mobile-menu' ),
    'type'      => \Elementor\Controls_Manager::COLOR,
    'default'   => '#1a1a1a',   // matches --ddmm-header-title-color
    'selectors' => [ '{{WRAPPER}}' => '--ddmm-header-title-color: {{VALUE}};' ],
] );
```

6. **Close Button Color + Close Hover Color (D-03 close hover):**
```php
$this->add_control( 'header_close_color', [
    'label'     => esc_html__( 'Close Button Color', 'devsroom-drilldown-mobile-menu' ),
    'type'      => \Elementor\Controls_Manager::COLOR,
    'selectors' => [ '{{WRAPPER}}' => '--ddmm-close-color: {{VALUE}};' ],
] );
$this->add_control( 'header_close_hover_color', [
    'label'     => esc_html__( 'Close Button Hover Color', 'devsroom-drilldown-mobile-menu' ),
    'type'      => \Elementor\Controls_Manager::COLOR,
    'selectors' => [ '{{WRAPPER}}' => '--ddmm-close-hover-color: {{VALUE}};' ],
] );
```

Then `$this->end_controls_section();`

**Pitfall checklist before committing:**
- [Pitfall 1] Every selector uses `{{WRAPPER}} .ddmm-…` with a SPACE. None use `{{WRAPPER}}.ddmm-…`.
- [Pitfall 3] All group-control `name` values unique: `trigger_border`, `trigger_text_typography`, `drawer_box_shadow`, `header_border`, `header_title_typography`. No collisions with Content Tab.
- [Pitfall 6] DIMENSIONS uses `{{TOP}}{{UNIT}} {{RIGHT}}{{UNIT}} {{BOTTOM}}{{UNIT}} {{LEFT}}{{UNIT}}` — one UNIT per number.
- [Pitfall 7] Responsive `default` arrays use a single `size` + `unit` only (no per-breakpoint defaults).
- [Pitfall 9] Every COLOR/SLIDER `default` value matches the corresponding `--ddmm-*` var default from Task 1 (case-sensitive: lowercase hex).
- [Pitfall 5] `separator => 'before'` on the FIRST section only (`section_style_trigger`); NOT on `section_style_drawer` or `section_style_header`.

Use 4-space indentation (match the existing `_register_controls()` style). Do NOT touch any Content Tab code — append only.
  </action>
  <verify>
    <automated>php -l src/Elementor/Widget/DrillDownMenu.php</automated>
  </verify>
  <acceptance_criteria>
- `php -l src/Elementor/Widget/DrillDownMenu.php` exits 0 (No syntax errors detected)
- `grep -cE "section_style_trigger" src/Elementor/Widget/DrillDownMenu.php` returns ≥ 1 (STYL-01)
- `grep -cE "trigger_(color|bg|hover_color|hover_bg|padding|border|text_typography)|hamburger_width" src/Elementor/Widget/DrillDownMenu.php` returns ≥ 8 (STYL-01 control inventory)
- `grep -cE "start_controls_tabs.*trigger_state_tabs" src/Elementor/Widget/DrillDownMenu.php` returns ≥ 1 (D-03 trigger hover)
- `grep -cE "section_style_drawer" src/Elementor/Widget/DrillDownMenu.php` returns ≥ 1 (STYL-02)
- `grep -cE "drawer_(width|bg|box_shadow)" src/Elementor/Widget/DrillDownMenu.php` returns ≥ 3 (STYL-02)
- `grep -cE "overlay_color" src/Elementor/Widget/DrillDownMenu.php` returns ≥ 1 (STYL-02)
- `grep -cE "'px', *'vw', *'%'" src/Elementor/Widget/DrillDownMenu.php` returns ≥ 1 (STYL-02 size_units)
- `grep -cE "responsive.*=>.*true" src/Elementor/Widget/DrillDownMenu.php` returns ≥ 2 (drawer_width + hamburger_width, D-05)
- `grep -cE "section_style_header" src/Elementor/Widget/DrillDownMenu.php` returns ≥ 1 (STYL-03)
- `grep -cE "header_(bg|border|height|title_typography|title_color|close_color)" src/Elementor/Widget/DrillDownMenu.php` returns ≥ 6 (STYL-03 control inventory)
- `grep -cE "header_close_hover_color" src/Elementor/Widget/DrillDownMenu.php` returns ≥ 1 (D-03 close hover)
- (negative) `grep -cE "selectors.*=>.*\[\s*\]" src/Elementor/Widget/DrillDownMenu.php` returns 0 (no empty selectors arrays)
  </acceptance_criteria>
  <done>
Three Style Tab sections (Trigger STYL-01, Drawer STYL-02, Header STYL-03) committed. PHP lints clean. Every required control key exists. Drawer width is responsive (D-05) and supports px/vw/% (STYL-02). Trigger padding uses per-side Dimensions (D-06). Trigger/Close hover tabs/controls present (D-03). Typography group controls on trigger text and header title (D-02). The sections append to the existing `_register_controls()` without touching Content Tab code.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Author → Elementor editor | The author picks color/size values in the Style tab; Elementor sanitizes these via its CSS-generation pipeline before emitting per-instance CSS. No raw author input reaches frontend HTML. |
| Editor preview → Author | The editor preview echoes static sample strings wrapped in `esc_html__()` — already established. This plan adds NO new PHP echo paths (only `add_control`/`add_group_control` data-structure calls). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01-01 | Tampering (XSS via control default values) | `DrillDownMenu::_register_controls()` control definitions | accept | All defaults are static strings wrapped in `esc_html__( …, 'devsroom-drilldown-mobile-menu' )`. No user input flows into control definitions. Elementor's CSS-generation pipeline sanitizes selector values. Matches ASVS V5.1.1 — output encoding enforced by the framework. |
| T-06-01-02 | Tampering (CSS injection via selector values) | Style Tab `selectors` arrays | accept | Selectors are hardcoded PHP strings keyed off `{{WRAPPER}} .ddmm-<bem>` — never user-supplied. The author's chosen color/size VALUE flows through `{{VALUE}}`/`{{SIZE}}{{UNIT}}` tokens which Elementor validates against the control type. No `!important`, no eval, no `style="…"` echo. |
| T-06-01-03 | Information Disclosure (PLUG-06 carry) | `_register_controls()` new code | accept | This plan adds only control-registration calls; no `echo`, no `printf`, no dynamic output. The PLUG-06 escaping pattern is upheld by construction — there is no unescaped output path in this plan's diff. |

No `[BLOCKING]` threats. No new runtime dependencies. Reuses established escaping patterns from Phase 4/5.
</threat_model>

<verification>
**Per-task automated (run after each task):**
- Task 1: `grep` counts on `ddmm-frontend.css` for the new `--ddmm-*` hooks (see acceptance_criteria).
- Task 2: `php -l src/Elementor/Widget/DrillDownMenu.php` exits 0 AND every STYL-01/02/03 grep in 06-VALIDATION.md returns the expected count.

**Plan-level (run after Task 2):**
- `find src -name '*.php' -exec php -l {} \;` — full lint sweep, no errors.

**Deferred to /gsd-verify-work (manual, browser-only — no automated path exists):**
- Open the widget in Elementor editor → Style tab → confirm `Trigger Button`, `Drawer`, `Header` sections render with the expected controls.
- Set distinctive values (loud color, large padding) in each section → confirm they appear in the editor preview AND the published frontend drawer.
</verification>

<success_criteria>
- [x] STYL-01: Trigger section has COLOR, SLIDER (hamburger), DIMENSIONS (padding), Group_Control_Border, Group_Control_Typography, Normal/Hover tabs — all grep checks pass
- [x] STYL-02: Drawer section has SLIDER (width px/vw/%), COLOR (bg), Group_Control_Box_Shadow, COLOR (overlay alpha) — all grep checks pass
- [x] STYL-03: Header section has COLOR (bg), Group_Control_Border, SLIDER (height), Group_Control_Typography (title), COLOR (title), COLOR (close) — all grep checks pass
- [x] D-01: Refined baseline — softer menu-border-color, drawer box-shadow var, refined header padding — committed
- [x] D-02: Trigger text + Header title Typography group controls present
- [x] D-03: Trigger hover (tabs) + Close hover (control) present
- [x] D-05: Drawer width + Hamburger width are responsive
- [x] D-06: Trigger padding is per-side Dimensions
- [x] PHP lint clean, no syntax errors
</success_criteria>

<output>
After completion, create `.planning/phases/06-style-tab-controls/06-01-SUMMARY.md` documenting:
- The exact refined `--ddmm-*` default values committed (for Plan 06-03 to verify against)
- The complete list of new Style Tab control keys (for Plan 06-02 to avoid name collisions per Pitfall 3)
- Any deviations from this plan's prescribed control inventory (with rationale)
</output>
